  Work through TASKS.md top to bottom. One step, one commit — never combine
  steps into a single commit. Where a step says "red"/"green", commit the
  failing test before writing the implementation. After each commit, stop
  and wait for review before starting the next step. Check off the
  completed item in TASKS.md as part of that same commit.